{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "postinstall": "npm run build && php artisan optimize",
        "start": "php artisan serve --host=0.0.0.0 --port=${PORT:-8000}"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}
